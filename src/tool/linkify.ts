import { ToolDefine } from '../types';

const linkify: ToolDefine = {
    icon: 'linkify',
    addFormat: () => {},
    removeFormat: () => {},
    isFormatted: () => false,
};

export default linkify;
