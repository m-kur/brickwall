import { ToolDefine } from '../types';

const markerTool: ToolDefine = {
    icon: 'edit',
    addFormat: () => {},
    removeFormat: () => {},
    isFormatted: () => false,
};

export default markerTool;
